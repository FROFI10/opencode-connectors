{
  "name": "@opencode-connectors/template",
  "version": "0.1.0",
  "private": true,
  "description": "Template MCP connector for OpenCode. Copy this folder and rename to start a new connector.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "opencode-connector-template": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "start": "node dist/index.js",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
